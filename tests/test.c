#include <stdio.h>
#include <stdlib.h>

int main() {
    // printf("Hello world!");
    int a, b, c;

    scanf("%d %d %d", &a, &b, &c);

    b = b * c;

    printf("Here ya are mayait %d\n", b);

    // int* p = (int*) malloc(20 * sizeof(int));

    // A[3] = p[4];

    // int* q = (int*) malloc(13 * sizeof(int));

    // p[2] = q[0];

    // free(p);

    return 0;
}